.next/
node_modules/
.env.local
.DS_Store

# TypeScript incremental build cache — should not be in git
tsconfig.tsbuildinfo
*.tsbuildinfo

# Local Claude Code config (per-machine, not shared)
.claude/

# Vercel CLI link metadata
.vercel

# Local script logs (per-machine)
scripts/logs/
